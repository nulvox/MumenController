use usb_device::class_prelude::*;
use usb_device::prelude::*;
usbd_human_interface_device::hid_class;

// Data structure for storing the report data as packaged by Switch Gamepads
pub struct KeyData {
    buttons: u16,
    hat: u8,
    padding: u8,
    lx: u8,
    ly: u8,
    rx: u8,
    ry: u8,
}

